# Structures

```
struct Point:
    var:int x, y

func pointToString(Point point):
    return "(" + point.x + ", " + point.y + ")"

func main():
    var point = Point(1, 1)
    >> pointToString(point)

```

[Index](index.md) | [<< Back](16_enumerators.md)